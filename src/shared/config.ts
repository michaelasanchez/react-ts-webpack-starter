const API_DOMAIN = 'localhost';
const API_PORT = 0;
const API_PATH = '';

const API_URL = `https://${API_DOMAIN}:${API_PORT}/${API_PATH}`;

const CLIENT_ID = '';
const COOKIE_POLICY = 'single_host_origin';

export const config = {
  apiUrl: API_URL,
  clientId: CLIENT_ID,
  cookiePolicy: COOKIE_POLICY,
};
